import styled from "styled-components";

const Test = () => {
  return (
    <Wrapper>
      <span>jsx example</span>
    </Wrapper>
  );
};

export default Test;

const Wrapper = styled.div`
  // CSS
`;